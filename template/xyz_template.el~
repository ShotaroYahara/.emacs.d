;; 本当はここに最終更新日とか入れたい…たい（今度ね）


;; インストール設定　してなかったらインストールする
(unless (package-installed-p '#packagename#)
  (package-install '#packagename#))


;;読み込み設定
(autoload '#packagename#)
(require '#packagename#)
